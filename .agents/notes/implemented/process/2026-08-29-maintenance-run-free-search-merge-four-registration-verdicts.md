# Agent Note: Maintenance run merges free-search at rank 44 and issues four-registration verdicts

Status: implemented

## Problem

Follows [the preceding record](2026-08-29-maintenance-run-passwords-merge-free-search-approval.md) of the same day, under the default scope (open PRs whose assignees include zhu1090093659; Issues out of scope). Nine open PRs were in scope: the approved dsh-free-search registration waiting on its rank rebase, three threads already carrying maintainer feedback, two parked ones, and four brand-new index registrations (#1277, #1279, #1282, #1285) whose heads all still claimed the not-yet-awarded slot. The run had to award rank 44 first-complete-first-served, re-verify the waiting threads, and deliver the mandatory three-axis verdicts (utility, stability, compatibility) on the four new registrations — including a cohort check against the running 0.1.2-alpha.1 profile, because host SDK seams decide whether a third-party plugin loads at all.

## Decision

- #1249 (DDDMUC, dsh-free-search): verified and merged as 610131ebc. The 09:22 UTC force-push (f60861952) is the expected rank-44 rebase onto 89829ef21, a two-file +25/-0 diff; on a detached worktree at the head, `node scripts/community-index --check` passed (44 entries) and `node scripts/market-build --check` passed (tryon/ hash manifest 756 files, dist up to date). All required checks were green and the 03:25 approval survived the push, so the PR was squash-merged; the merged manifest tail reads dsh-passwords 43, dsh-free-search 44.
- #1098 (Sivan757, dsh-agent-plugins-market): the two outstanding log excerpts arrived and close the evidence loop — a dsh web restart (PID 12267 to 29636) restored the lifecycle-hooks suite and MCP/hooks mounts per enabled state, and a PreToolUse fixture log shows firing while enabled, zero additions after disable, firing on re-enable, and state cleared on uninstall. The head's CI and agent-notes-guard runs were released from `action_required`. One item remains: rebase to rank 45, since 44 is taken.
- #1100 (termanli, dsh-fulltext-search): no new commits since 2026-08-28; a comment re-points the slot from 44 to 45 alongside the standing blank-line fix.
- Four new registrations reviewed in depth. All are single-commit +25/-0 index changes, all heads pass `community-index --check` and `market-build --check` locally, and none may merge at rank 44, so every verdict comment carries the rank-45 rebase instruction.
  - #1277 (Nath-Vikky, dsh-codekin): passes all three axes pending one description fix. Upstream CI builds against the official dsh-v0.1.2-alpha.1 tag and asserts the committed lib/ matches, with seven releases, lifecycle and engine tests, an enable/disable switch, and atomic capped persistence; all eight client.inject packages were verified present in the local 0.1.2-alpha.1 cohort store, and the engines range correctly brackets the cohort. The one defect: the entry says game state stays "local to the plugin" while the actual location is `$DSH_HOME/tracewild/state.json` with no uninstall cleanup found, so the description must state the real path and the surviving file.
  - #1279 (Jamsharden, dsh-reasoning-effort): utility confirmed (no duplicate entry; real gap for third-party API users), blocked on declarations rather than code. There is no dsh.engines or README cohort statement (the three injected client packages were verified present in alpha.1), and no uninstall disclosure for the llm-pi-ai settings fields the plugin writes, which persist after removal. Informational: npm 0.2.0 and 0.2.3 gitHeads point at commits absent from the single-commit repository created today, so their source provenance is unverifiable.
  - #1282 (rongxingda, dsh-prompt-enhance): the strongest implementation of the four (auxiliary calls through ctx.llm with harness credentials, loopback-fenced route, refuse-not-truncate input bounds, draft undo stack, documented uninstall step, green CI), but a hard compatibility blocker: `@deepseek-ai/dsh-client-runtime` does not exist in the 0.1.2-alpha.1 cohort store (removed upstream), while the plugin's dsh.client.inject requires it and its engines floor stops at >=0.1.1-rc.1 — the client inject cannot resolve on the running cohort. An alpha.1-tested release with corrected inject and engines is required before listing.
  - #1285 (GreenLv, dsh-completion-guard): high engineering quality (five tags, CI matrix, test suite, PRIVACY and COMPATIBILITY docs, a clean rename away from a colliding third-party name), blocked on cohort verification: host peers are pinned to ^0.1.1-rc.2 while the profile runs 0.1.2-alpha.1, and the hooked seams (agent/session-start, agent/pre-step, agent/turn-stopping, tools.guard, update_goal deny) have no alpha.1 verification record even though the five host packages exist there. The entry description also needs fixing: the "eight lifecycle hooks" phrasing does not match the inspected seams, and half-width punctuation is used where the index uses full-width.
- #1144 (deepsea) and #1245 (tokyo-night): no author movement since the last round; parked on their recorded blockers.

## Alternatives considered

- Merging #1277 immediately at rank 45 as the earliest complete push: rejected — its head still claims rank 44 and would corrupt the manifest ordering; the description fix rides the same push anyway.
- Rewriting the stale-rank rebases maintainer-side to land several registrations in one round: rejected for the authorship and regeneration reasons already recorded in the preceding note.
- Treating #1282's missing dsh-client-runtime as non-blocking because the rc.2 cohort still ships it: rejected — the index serves the running cohort; a plugin whose client half cannot resolve its inject on alpha.1 ships a broken install path for current users.
- Requiring recoverable source history for npm 0.2.0/0.2.3 as a hard blocker on #1279: declined — it is a provenance-transparency question with an easy answer; the blocking items are the cohort declaration and the uninstall disclosure.

## Consequences

- dev advanced 89829ef21 to 610131ebc carrying the dsh-free-search registration (44 entries, rank 44); the next free slot is 45. Six PRs (#1098, #1100, #1277, #1279, #1282, #1285) are all converging on rank 45, so the next runner must re-read every thread and award the slot first-complete-first-served.
- A reusable review fact is now on record: the 0.1.2-alpha.1 cohort store under `~/.dsh-cohorts/0.1.2-alpha.1/` contains no dsh-client-runtime, and inject lists can be checked against that store directly.
- Verification worktrees and node_modules links under /tmp were removed after use. Local dev carries six unrelated unpushed commits from another session and was left alone; this note reached origin/dev from a temporary branch, and local dev will integrate it with the user's next push or pull.
