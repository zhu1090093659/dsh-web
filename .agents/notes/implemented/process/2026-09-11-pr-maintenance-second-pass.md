# Agent Note: PR maintenance run 2026-09-11 (second pass) — two skin-side PRs merged, three registrations returned, one approval waiting on CI

Status: implemented

## Problem

Second maintenance pass of the day on `zhu1090093659/dsh-web`, covering the nine open PRs assigned to the maintainer account: five awaiting a first or renewed review (#1489 ice-princess skin, #1484 whalechan-harness skin, #1476 blue-fantasy readability follow-up, #1488 bwm-friend plugin registration, #1479 voice-talk plugin registration), one feedback-update re-review (#1467 dsh-thread-tools, answered after the morning pass), and three still author-blocked (#1399, #1321, #1318). Plugin registrations carry the mandatory three-axis review (usefulness, stability, compatibility) with local evidence; visual changes need the screenshots actually inspected, not just present.

## Decision

#1489 and #1476 are reviewed, approved, and merged into `dev` as merge commits (`08d5869c`, `b890a796`). #1488 and #1484 are approved with merge blocked on the required CI/rebase. #1479 and #1467 are returned with changes requested. The three author-blocked registrations are untouched.

#1489 ice-princess — detached-worktree gates all green (`market-build --check`, `skin-center:check`, `i18n:check`, `docs:check`, `dsh-skin validate` PASS, no hash-suffix selectors). Both previews inspected; the disclosed 0% light/dark pixel difference is intentional (dark-only design, same precedent as #1464). Apache-2.0 and the background-art attribution are declared in `skin.json`. Merged.

#1476 blue-fantasy follow-up — the same gate set green, validate PASS with the four known hash-selector warnings recorded since #1468. The eight changes each carry a reasoned trade-off, including two rejected experiments. Both evidence screenshots inspected: readability on the illustration clearly improves. A non-blocking observation is recorded: light-theme code blocks and turn-status/footer rows stay faint. Merged.

#1488 bwm-friend — three-axis review with real evidence. Mount probe in an isolated `DSH_HOME` on the current host line (dsh 0.1.5-rc.1, plugin pinned to npm 0.6.0): boot clean, `/bwm-friend/health` returns `{"ok":true,"version":"0.6.0"}`, native root path unaffected, the companion page renders fully styled. Code-level boundary check: the host-side `DEEPSEEK_API_KEY` read from `~/.dsh/.credentials.yaml` is sent only to api.deepseek.com; MiMo voice fires only with `XIAOMI_MIMO_API_KEY`; the cloud login/update paths (BWM_AUTH_BASE / BWM_UPDATE_URL) stay dormant unconfigured. cordis patch is insert-only. Approved — but the required CI caught a contract failure my local gates missed: `scripts/market-layout.test.mjs` (plugins.json 契约) requires a `subcategory` on every categorized entry, and the bwm-friend entry has none. The fix request (suggest `"subcategory": "chat"`) is posted; merge stays blocked until checks are green.

#1484 whalechan-harness — the same skin gate set green; validate PASS with two hash-selector warnings (accepted precedent). Previews inspected: clean, restrained blue design with the character as a corner watermark; CC BY-NC-SA 4.0 and NOTICE declared. Approved, but its CI runs the pre-existing `packages/dsh-ssh` ProxyCommand EPIPE failure (the branch predates the 12:36 dev fix) plus an old-base task-board assertion; the author is asked to rebase onto `origin/dev` and let CI rerun before merge.

#1479 voice-talk — returned. Hard boot incompatibility: the plugin (both registered 0.1.1 and current latest 0.2.0) imports `installSettingsSection` / `settingsNamespace` from `@deepseek-ai/dsh-settings` at module top, and the 0.1.5-rc.1 host SDK does not export them, so the loader fails and **the whole profile boot aborts**, not just this plugin. The declared peer range `^0.1.0-rc.7` (<0.2) covers the current line, so the declaration overclaims. The index-side work is clean (community-index OK, market-build regeneration zero-drift) and the credential boundary is clean (keys stay host-side and flow only to DashScope/xfyun websocket endpoints). Upstream also shipped 0.2.0 after the PR narrative froze at 0.1.1 — the entry carries no version pin, so the Workshop would install latest; recorded to the author.

#1467 dsh-thread-tools (feedback update) — all three morning blockers verified fixed: npm field removed (git install via committed `lib/`), stability evidence added (keyless npm test, GitHub Actions, v0.1.0 tag and release), template complete and checks green. Worktree re-verification: community-index OK (59 entries), index tests 9/9, market-build regeneration zero-drift. The action_required CI run was maintainer-approved. The contributor's claim that the red CI was the pre-existing ssh flake is confirmed (fixed on dev by `fix(ssh)` run 34599812650). Remaining blocker: `dsh.engines.dsh >=0.1.2-rc.1 <0.2` numerically includes the 0.1.5 line the package's own llms.txt explicitly does not claim; the host does not enforce the field, so a current-line user would install successfully into a host the package does not support. Asked to either adapt to the 0.1.5 line (preferred) or narrow the declaration to the verified line.

#1399, #1321, #1318 have no author commits or replies since their changes-requested reviews and stay as they are.

One toolchain finding recorded for future probes: pnpm's default minimum-release-age gate makes a freshly published version unresolvable for roughly a day — `dsh plugin add dsh-plugin-bwm-friend` resolved 0.3.1 while dist-tags.latest was 0.6.0, on both the CLI-bundled pnpm 11.9 and the shell pnpm 11.26 with fresh metadata caches. `--config.minimum-release-age=0` restores current-version resolution. This is environment behavior, not a package defect; probes should pin the version under review explicitly.

## Alternatives considered

Merging #1488 with administrator privileges over the failed required check was rejected: the ruleset requires green checks and the maintenance rules prohibit merging while any required gate fails; the one-line fix is trivial for the active author. Pushing the subcategory fix directly to the contributor's fork was rejected as intrusive when the author had pushed hours earlier and a comment reaches them mid-session. Blocking #1476 on the faint light-theme rows was rejected: every changed element is strictly more readable than the merged base, so the observation is recorded for a follow-up instead. Blocking #1484 on its CI was rejected on blame — both failures are proven pre-existing and already fixed on dev — but merging without green required checks is prohibited, so the rebase path is mandatory. Blocking #1489 on the pixel-identical light theme was rejected: the intent is disclosed in the PR, the dark design is the product, and #1464 set the precedent.

## Consequences

`dev` carries the blue-fantasy readability follow-up (`b890a796`) and the ice-princess skin (`08d5869c`; catalog 33 repo skins). Four PRs stay open pending author action: #1488 (one-line subcategory), #1484 (rebase onto current dev), #1467 (engines declaration honesty), #1479 (0.1.5-line adaptation). Three registrations remain author-blocked. The ssh EPIPE flake recorded as an open gap in the morning pass is closed on dev by `e92a7f56`. Registration reviews in this repository must include `node --test scripts/market-layout.test.mjs` alongside community-index and market-build — community-index alone does not enforce the subcategory contract.

## Testing

Per-PR detached worktrees with node_modules symlinked from the main checkout. Skins (#1489, #1484, #1476): `node scripts/market-build --check`, `node scripts/skin-center-catalog-check --check`, `node scripts/i18n-audit.mjs --check`, `node scripts/verify-docs.mjs`, `node scripts/dsh-skin validate <dir>`, plus direct inspection of the preview/evidence images. Registrations (#1488, #1479, #1467): `node scripts/community-index`, `node scripts/community-index --check`, `node scripts/market-build` (regeneration drift must be zero), `node --test scripts/community-index.test.mjs`, and for #1488 additionally `scripts/market-layout.test.mjs` reproduced the CI failure. #1488's mount probe ran `dsh --profile <probe> --port 3791 --host 127.0.0.1 --no-open` with an isolated `DSH_HOME` and plugin 0.6.0, then curl-checked `/bwm-friend/health` (200, version 0.6.0), the native root (401 without token / 303 with), and screenshotted the companion page headlessly; the probe process was stopped after verification. #1479's probe reproduced the boot abort on the current host line. Stuck `action_required` workflow runs for #1489's, #1484's, #1467's, and #1488's branches were approved so the full gates could run.
