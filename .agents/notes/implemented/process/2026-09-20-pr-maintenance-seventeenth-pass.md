# Agent Note: PR maintenance run 2026-09-20 (seventeenth pass) — merge the Doro pet and the 32-plugin PerryLink community index batch

Status: implemented

## Problem

Seventeenth maintenance pass on `zhu1090093659/dsh-web`, following the [sixteenth pass](2026-09-20-pr-maintenance-sixteenth-pass.md). Default scope: the open PRs assigned to maintainer account `zhu1090093659` — no Issue scan. Nine PRs were open and in scope. #1630 (the Doro pet) had answered its change requests and now merged cleanly, #1647 (the remaining 32 PerryLink community plugins) was new, and seven carried-over PRs (#1626, #1607, #1603, #1526, #1488, #1479, #1399) still stood open on unanswered change requests. No collaborator review existed on any of them; every formal review came from the authenticated maintainer account itself, so the "already reviewed by a collaborator" skip rule did not apply.

## Decision

Two PRs merged; the other seven confirmed still author-blocked.

- #1630 (Doro pet with wash mode, random roaming and drag struggle, by stushansusu): merged as `9556103a9`. The sixteenth pass had already approved the content and asked only for a rebase; the two commits that arrived afterwards were regenerated artifacts (`691f939a`) and test-standards fixes (`a8e75343`), neither of which changes behavior, so the standing approval was re-verified against the new head rather than re-litigated. All three pet content gates were re-checked on that head: evidence (the in-PR track overview, the struggle frame strip and the per-track previews plus the registry API response the author supplied — accepted on the same basis as the fifteenth and sixteenth passes, not as the settings-page screenshot the gate literally asks for), provenance (`packages/dsh-pet/THIRD_PARTY_NOTICES.md` now records that Doro is an unofficial fan derivative of Dorothy from *Goddess of Victory: Nikke*, that the character and related rights belong to SHIFT UP Corp., and that the assets are personal non-commercial use only, matching the dsh-pet README pair and the root README table), and aesthetics (direct multimodal inspection of the overview, the struggle strip and the idle / work-success / wash / move previews: consistent character, clean alpha, correct props per track, no clipping, distortion or watermark).
- #1647 (the remaining 32 PerryLink community plugins, by PerryLink): three-axis reviewed, approved and merged as `2e21cd635`. Usefulness: no row duplicates one of the 85 entries already registered; the rows that sit near an existing capability (dsh-memento beside the existing memory entries, dsh-draw beside dsh-imagegen, dsh-session-sync beside dsh-cloud-sync, dsh-talk beside dsh-audiogen, dsh-fast beside dsh-context and dsh-usage-panel) differ in mechanism and scope, and every category and subcategory is an existing enum value. Stability: all 32 repositories are public, unarchived and Apache-2.0, carry the `dsh-plugin` topic and a root `cordis.patch.yml`, were pushed on 2026-09-19, publish on npm with a `repository.url` matching the registered repository, ship `cordis.patch.yml` + `package.json` + LICENSE in the tarball, and run green CI and plugin-doctor workflows with 10 to 50 test files each. Compatibility: every declared peer range admits the repository baseline `@deepseek-ai/cordis ^4.0.2` and `@deepseek-ai/dsh-* ^0.1.5-rc.1` (checked per range with semver), and no index id or npm name collides.
- Residual advisories were recorded on the PR instead of blocking, following the sixteenth pass's treatment of the previous PerryLink batch: `dsh-team-rooms` and `dsh-background-agents` are mutually exclusive (same tool names, `settings.section` id and storage domain) yet the store lists both with no conflict hint; `dsh-skill-pack-security` keeps its `cordis.patch.yml` under `provider/` rather than at the repository root, so a git-channel install must target that subdirectory; `dsh-plugin-kit` ships an intentionally empty patch because it is a library rather than a mountable plugin; `dsh-output-styles` pins `@deepseek-ai/dsh-storage{,-json,-domain}` 0.1.5-rc.2 as hard dependencies while its peer ranges exclude the 0.1.6 line; several packages declare build tooling under `dependencies` rather than `devDependencies`; and the released `dsh-budget` 0.4.9 still carries the cache-hit pricing defect tracked as its issue #4.
- Seven carried-over PRs (#1626, #1607, #1603, #1526, #1488, #1479, #1399) stay author-blocked: for each one the head SHA equals the commit its most recent review targeted, so there was no new work to review and the standing change requests stand. #1399 is still a draft.

## Alternatives considered

Splitting #1647 into several smaller batches, or declining the 32-row batch for size alone, was rejected: the index documents no cap, the rows are one author's family registered in the same shape as the seven-row batch already merged, and the whole batch was verifiable in one pass against the evidence the registration contract asks for. The contributor's offer to split is recorded on the PR, but batch size is not by itself a defect.

Blocking #1647 on the residual findings was rejected: the index stores links rather than vendored code, an entry whose upstream is a library or pins a dependency is still registrable, and the `dsh-team-rooms` / `dsh-background-agents` conflict is declared by its own upstream. This is the same reasoning the sixteenth pass applied to the previous PerryLink batch.

Mounting all 32 plugins in a local profile before merging was rejected: a link-only index entry neither vendors nor mounts the plugin, and doing it would mean installing 32 third-party bundles plus their external prerequisites (Ollama, language servers, IM credentials, a Windows-only native helper) onto the shared machine. The verification actually performed covers repository identity, npm publication, tarball contents, peer-range admission and CI, and the unverified boundary is stated both here and on the PR.

Re-reviewing the seven carried-over PRs because time had passed was rejected: their heads are unchanged since their last review, and issuing a fresh verdict on identical code would move the review state without adding evidence.

## Consequences

`origin/dev` now carries `9556103a9` (#1630) and `2e21cd635` (#1647), and local `dev` was fast-forwarded to `2e21cd635`. The community index grows from 85 to 117 entries, and `packages/dsh-pet/assets/doro` becomes the eighth repository-side built-in pet. Seven PRs remain open and author-blocked. No reviewed change touched the Wallpaper Engine / renderer paths, so no collaborator notification was required. Recorded rather than fixed: the `dsh-team-rooms` / `dsh-background-agents` mutual exclusion is user-visible in the store and stays an upstream-documentation matter unless the index later grows a conflict field. The `maint-1630` and `maint-1647` task branches remain after their worktrees were reclaimed, because `wt remove` would only delete them under `-D`, which this workflow does not authorize. Local `dev` carries this maintenance-note commit unpushed, since the current request did not ask to publish it; every accepted PR commit reached `origin/dev` through GitHub.

## Testing

Each PR was verified in its own `wt` worktree created from `origin/dev`, with the PR head merged in before testing.

- #1630 (`maint-1630`): merge of `refs/pull/1630/head` clean; `node scripts/dsh-pet validate packages/dsh-pet/assets/doro` valid; `pnpm --filter @linxin666/dsh-pet typecheck` and `build` clean; the dsh-pet suite 44 files / 539 tests passed; `node scripts/market-build --check` dist up to date (3230 files); `node scripts/lib-artifact-check.mjs` OK; `pnpm aggregate:check` OK; `pnpm test:standards` OK; `node scripts/verify-docs.mjs` clean. The 802-frame asset inventory, the eleven tracks and the registry entry were confirmed from the merged tree, and `loadPetRegistry` resolves the pet from `assets/*` so it appears without a code change.
- #1647 (`maint-1647`, base `origin/dev` at `9556103a9`): merge of `refs/pull/1647/head` clean; `node scripts/community-index` OK (117 entries); `node scripts/market-build --check` dist up to date (3230 files); `pnpm market:check` OK; `node scripts/verify-docs.mjs` clean; `pnpm i18n:check` OK; `pnpm emoji:check` OK; `pnpm test:scripts` 343/343 passed. Upstream evidence for all 32 rows was gathered read-only — `gh api` for repository, license, topic, patch, workflows, tests and issues; `npm view` and `npm pack --dry-run` inside temp directories for publication, identity and tarball contents; and semver range checks against the baseline.
- GitHub state was re-read after each approval and before each merge: `mergeStateStatus` `CLEAN`, `reviewDecision` `APPROVED`, and every required status check (`CI checks`, `plugin-mount`, `Validate PR contribution evidence`, `guard-agent-notes`) green.
- Not verified: none of the 32 plugins was mounted and run in a live profile, and #1630's pet was not switched to inside the running GUI, so upstream runtime claims rest on the upstream READMEs, their test trees and their CI rather than on local execution.
