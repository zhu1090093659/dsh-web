# Agent Note: PR maintenance run 2026-09-20 (sixteenth pass) — merge the whale-maid skin, the PerryLink community index and the reasoning-effort metadata; approve the doro pet pending a rebase

Status: implemented

## Problem

Sixteenth maintenance pass on `zhu1090093659/dsh-web`, following the [fifteenth pass](2026-09-20-pr-maintenance-fifteenth-pass.md). Default scope: the open PRs assigned to maintainer account `zhu1090093659` — no Issue scan. Four PRs needed action: #1631 (Whale Girl / Sea Gaze skin) and #1630 (Doro pet) had answered the fifteenth pass's change requests with new commits, and #1643 (seven PerryLink community plugins) plus #1641 (reasoning-effort entry metadata for 0.2.7) were new. Seven carried-over PRs (#1626, #1607, #1603, #1526, #1488, #1479, #1399) remained open with standing change requests.

## Decision

Three PRs merged, one approved and waiting on the author's rebase, and the carried-over queue confirmed still author-blocked.

- #1631 (Whale Girl · Sea Gaze skin by stushansusu): approved and merged as `9f3cd753`. The license change request is satisfied: the skin directory carries `LICENSE` (CC BY-NC-SA 4.0 full text), `skin.json` declares `license` / `licenseUrl` / `attribution`, the skin README pair has a license section, and a row was added to the root `README.md` / `README.en.md` copyright tables — the table lives in the root README, not the skin-center README, which the contributor adapted to correctly. Both evidence gates hold: the in-situ light and dark chat-window screenshots show readable body text, tool rows and composer over the translucent artwork, and the multimodal aesthetic check found no clipping, distortion, watermark or low-quality assets.
- #1643 (seven PerryLink plugins by PerryLink): approved and merged as `ebba8bd5` after the mandated three-dimension analysis for third-party plugin index registrations. Usefulness: seven non-overlapping, substantive plugins (GitHub tooling and a review bot, an MCP console, a local document library, a TickTick bridge, a plugin doctor, a plugin upgrade corridor, a certification MCP) whose metadata matches the upstream repositories and npm names, with no duplicate of an existing entry. Stability: all seven repositories are public, unarchived, Apache-2.0 with a LICENSE, and published on npm with a matching `repository.url`; `npm pack --dry-run` confirms every published tarball really ships `cordis.patch.yml`, `package.json` and a LICENSE, so `dsh plugin` can identify and mount them; `dsh-plugin-upgrade`'s 0.1.x and 2.0.x lines were published by one maintainer under one `repository.url`, so there is no npm name transfer. Compatibility: every peer range admits the repository baseline 0.1.5-rc.1. Residual advice was recorded on the PR instead of blocking: `dsh-mcp-panel` pins `@deepseek-ai/dsh-subprocess@0.1.6-alpha.1` as a production dependency, none of the seven declares `dsh.engines.dsh`, `dsh-cert-mcp` has a red MCP-registry publish workflow, `dsh-plugin-doctor`'s badge workflow fails on its own 401 lookups, and three packages ship build tooling as runtime dependencies.
- #1641 (reasoning-effort entry metadata for 0.2.7 by Jamsharden): approved and merged as `5c277196`. Unlike the two skin and pet content gates, this is a metadata refresh of an existing entry, so the check was whether the new description matches the published artifact: `lib/official-levels.json` inside `@megen-lebar/dsh-reasoning-effort@0.2.7` holds exactly 961 model records with 737 marked `reasoning: true`, and every named model family in the copy is present in that data.
- #1630 (Doro pet with wash mode, roaming and drag struggle by stushansusu): content approved, merge blocked by branch state. The provenance change request is satisfied — `packages/dsh-pet/THIRD_PARTY_NOTICES.md`, the dsh-pet README pair and the root README tables now state that Doro is an unofficial fan derivative of Dorothy (`Goddess of Victory: Nikke`), that the character and related rights belong to SHIFT UP, and that the assets are personal non-commercial use only. The engine work was reviewed on its merits and holds: `modes` and `roam` parsing is fail-closed, persisted mode ids are re-validated against the manifest, a mode switch resets the restore carry, and the incidental fixes are real (the per-verb state copy that dropped `restoreCarryMs` / `incomeCarryMs`, and the idle-director phase guard that kept ambient acts from ever rolling). Its GitHub state is `CONFLICTING` and the conflicts are generated files only (`market/dist/manifest/*.json`, `packages/dsh-web-all/lib/client.js.map`, `scripts/lib-artifact-fingerprints.json`), so the review asks the author to rebase onto `origin/dev` and re-run `pnpm build && pnpm libs:write` plus `node scripts/market-build`; the approval stands because the ruleset does not dismiss stale reviews on push.
- Seven carried-over PRs (#1626, #1607, #1603, #1526, #1488, #1479, #1399) remain author-blocked: no commits since their last review, standing `CHANGES_REQUESTED`, and no replies. #1399 is still a draft.

## Alternatives considered

Merging #1630 by pushing the contributor's commits to `dev` from a locally resolved tree was rejected: the conflicts are generated artifacts and regenerating them locally is mechanical, but routing a third-party contribution through a maintainer branch bypasses the documented external-contribution path while the author is responsive, and that author had rebased the sibling PR #1631 within hours of the request.

Blocking #1643 on the missing `dsh.engines.dsh` field was rejected: `docs/plugins.md` asks every published plugin to declare it, but the index contract does not check it, several already-indexed entries — including this author's `dsh-auto-review` and `dsh-permission-rules` — omit it, and the index stores links rather than code. It is recorded as follow-up advice on the PR.

Treating `dsh-plugin-upgrade`'s 0.1.x-before-repository-creation timeline as a name-squatting risk was rejected after checking npm directly: both the 0.1.3 and 2.0.1 tarballs name the same maintainer and the same `repository.url`, so the package history is continuous.

## Consequences

Three PRs are merged into `origin/dev` (`9f3cd753`, `ebba8bd5`, `5c277196`) and integrated into local `dev` by `9f7fbe93b`; #1630 is approved and waiting on the author's rebase; seven carried-over PRs stay open and author-blocked. No change touched the Wallpaper Engine / renderer paths, so no collaborator notification was required. Local `dev` still carries unpushed commits from earlier sessions (four feature and test commits plus the maintenance-note commits), and no push was performed, because the current request did not authorize publishing that unrelated unpushed work; `origin/dev` nevertheless contains every accepted commit, since all three merges went through the GitHub merge queue.

## Testing

Every content gate was run in its own `wt` worktree created from the then-current `origin/dev` (`3b323e53`), with the PR head merged in before testing.

- #1631: `node scripts/dsh-skin validate packages/skins/skin-center/skins/whale-maid` PASS; `pnpm skin-center:check` OK (42 catalog skins); `node scripts/market-build --check` tryon verified (756 files) and dist up to date (2394 files); `node scripts/verify-docs.mjs` clean; skin-center suite 43 files / 662 tests passed.
- #1643: `node scripts/community-index` OK (85 entries); `node scripts/market-build --check` dist up to date; `npm pack --dry-run` for all seven packages confirmed a shipped `cordis.patch.yml`, `package.json` and LICENSE; `npm view` confirmed the published `dsh` manifest, peer ranges and repository URLs.
- #1641: `node scripts/community-index` OK (78 entries); `node scripts/market-build --check` dist up to date; the published `official-levels.json` was counted directly (961 models, 737 reasoning).
- #1630: `node scripts/dsh-pet validate packages/dsh-pet/assets/doro` valid; dsh-pet suite 44 files / 539 tests passed; `pnpm --filter @linxin666/dsh-pet typecheck` clean; `node scripts/market-build --check` dist up to date (3207 files); `node scripts/lib-artifact-check.mjs` OK; `pnpm aggregate:check` OK; `node scripts/verify-docs.mjs` clean; multimodal inspection of the track overview, the struggle frame strip and per-track previews.
- Integration: `git merge origin/dev` into local `dev` resolved two generated-artifact conflicts by regeneration (`pnpm build && pnpm libs:write`, `node scripts/market-build`), then `libs:check`, `market-build --check` (2394 files) and `aggregate:check` all pass; the four `wt` worktrees were removed with `wt remove --no-delete-branch`.
