本次发布包含 4 项新功能、6 项修复、8 项其他改动。

### 新功能

- [web-ui-all] 重新内置 dsh-better-sidebar（0.18.0-alpha.0，peer 对齐 alpha.2、inject 改用 dsh-client-modules）
- [community] 在社区插件索引注册 dsh-archived-chats ([#1299](https://github.com/zhu1090093659/dsh-web/issues/1299))
- [i18n] 新增 dsh-i18n 俄语语言包：注册 ru 语言并集中携带全部 15 个家族命名空间的 ru 字典
- [scripts] 新增 i18n 审计门禁（i18n:check，验证每个包 zh 键的 ru 覆盖）

### 修复

- [ci] 保留 e2e 挂载冒烟 URL 中的启动 token，避免 alpha.2 浏览器认证栅栏导致误判
- [i18n] 家族侧边栏条目改走 SDK locale 运行时，语言切换后无需刷新即更新标签
- [dsh-usage] 提供商错误列表的分隔符取自字典
- [dsh-git-graph] blocked-paths 列表的分隔符取自字典
- [dsh-remote-web-ui] 竖屏适配注入的标签（侧边栏按钮、紧凑选择器）本地化
- [dsh-perf] HUD 告警文案改走 locale 字典

### 其他改动

- [git] 新增 pre-push 守卫：origin 的 push URL 残留 fork 指向时拦截推送，防止静默推到错误仓库
- [readme] 从 GitHub API 同步贡献者列表
- [notes] 在 i18n 语言包笔记中记录家族侧边栏运行时 locale 的后续跟进
- 记录 ru 语言包机制与 i18n 维护契约（docs/i18n.md）
- [skills] 记录 alpha.2 升级中排除外部插件与挂载冒烟锚点的经验教训
- [notes] 记录 v0.3.9 挂载冒烟启动锚点修复
- [e2e] 挂载冒烟改为断言被排除的外部插件缺席（替代断言 better-sidebar 挂载）
- 为 lan-bind 补丁工具添加单元测试与 TypeScript 配置

<details>
<summary>English</summary>

This release contains 4 new features, 6 bug fixes, 8 other changes.

### New Features

- [web-ui-all] re-add dsh-better-sidebar to the aggregate (0.18.0-alpha.0, peers aligned to alpha.2, inject renamed to dsh-client-modules)
- [community] register dsh-archived-chats in the community plugin index ([#1299](https://github.com/zhu1090093659/dsh-web/issues/1299))
- [i18n] add the dsh-i18n Russian language pack: registers the ru language and centrally carries the ru dictionaries for all 15 family plugin namespaces
- [scripts] add the i18n audit gate (i18n:check, verifying ru coverage of every package's zh keys)

### Bug Fixes

- [ci] keep the launch token in the e2e mount smoke URL so the alpha.2 browser-auth fence cannot false-fail the lane
- [i18n] route the family sidebar rows through the SDK locale runtime so labels follow a language switch without a reload
- [dsh-usage] take the provider error list separator from the dictionary
- [dsh-git-graph] take the blocked-paths list separator from the dictionary
- [dsh-remote-web-ui] localize the portrait-adaptation injected labels (whale button, compact picker)
- [dsh-perf] route the HUD alert copy through the locale dictionary

### Other Changes

- [git] add a pre-push guard that blocks pushes when origin's push URL was left pointing at a fork, preventing silent pushes to the wrong repository
- [readme] sync contributor list from GitHub API
- [notes] record the family sidebar runtime-locale follow-up in the i18n language-pack note
- record the ru language pack mechanism and the i18n maintenance contract (docs/i18n.md)
- [skills] capture the excluded-externals and mount-smoke anchor lessons from the alpha.2 upgrade
- [notes] record the v0.3.9 mount-smoke boot-anchor fix for the excluded externals
- [e2e] assert excluded externals absent instead of better-sidebar mounting on the alpha.2 cohort
- add unit tests and TypeScript configuration for the lan-bind patch utility

---

Generated automatically by the release pipeline (scripts/release-notes.mjs).

</details>

---

由发布管线自动生成（scripts/release-notes.mjs）。
