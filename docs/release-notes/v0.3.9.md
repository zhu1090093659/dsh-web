本次发布包含 4 项新功能、8 项修复、12 项其他改动。

### 新功能

- [sdk] 将官方 SDK cohort 升级到 0.1.2-alpha.2——从 npm `alpha` dist-tag 解析，删除 source-built tarball store 机制
- [root] 以聚合别名 bundle 的方式把本仓库作为一个插件安装
- [community] 在社区插件索引注册 dsh-codekin
- [community] 在社区插件索引注册 dsh-reasoning-effort

### 修复

- [web-ui-all] 从启动中排除 alpha.2 不兼容的外部插件（dsh-better-sidebar、dsh-archive-manager）
- [task-board] 在限定的网关 service-unavailable 错误码上重试 session/list
- [remote-web-ui] 固定折叠帧列，让移动端竖屏中心列不再零宽
- [remote-web-ui] 手机端重开指向 service worker，而非 harness 的 401
- [market] 把密钥通过管道传给 wrangler，而非丢弃到继承的 stdin 上
- [market] 在 403 时暴露 siteverify 错误码，并在部署时断言 Turnstile 配对
- [market] 在部署时绑定刷新前修剪 TURNSTILE_SECRET
- [build] 让 cohort tarball overrides 适配相对路径并检测本地 harness

### 其他改动

- [release] 恢复 npm 发布通道（`NPM_PUBLISH_ENABLED` 翻回 `'true'`），CI 验证 DSH 钉点升到 0.1.2-alpha.2
- [notes] 记录删除被取代的 relative-cohort tarball store 决策
- [notes] 记录 alpha.2 排除外部插件的决策
- [sdk] 在主线 checkout 深度重新生成 lockfile
- [community-plugins] 在社区插件索引注册 dsh-agent-plugins-market
- [notes] 记录四项注册裁决维护运行
- [notes] 记录双档验收策略与四项注册落地
- [notes] 满足 turnstile 配对笔记的无表情门禁
- [readme] 从 GitHub API 同步贡献者列表
- [notes] 记录关于放弃 dsh-memory 注册的 owner 裁决

<details>
<summary>English</summary>

This release contains 4 new features, 8 bug fixes, 12 other changes.

### New Features

- [sdk] upgrade the official SDK cohort to 0.1.2-alpha.2 — resolved from the npm `alpha` dist-tag, dropping the source-built tarball store mechanism
- [root] install the repository as one plugin via an aggregate alias bundle
- [community] register dsh-codekin in the community plugin index
- [community] register dsh-reasoning-effort in the community plugin index

### Bug Fixes

- [web-ui-all] exclude alpha.2-incompatible external plugins (dsh-better-sidebar, dsh-archive-manager) from boot
- [task-board] retry session/list on the qualified gateway service-unavailable code
- [remote-web-ui] pin collapsed frame columns so the mobile portrait center is not zero-width
- [remote-web-ui] hand phone reopens to a service worker instead of the harness 401
- [market] pipe the secret into wrangler instead of dropping it on inherited stdin
- [market] surface siteverify codes on 403 and assert the Turnstile pairing at deploy time
- [market] trim TURNSTILE_SECRET before the deploy-time binding refresh
- [build] adapt cohort tarball overrides to relative paths and detect local harness

### Other Changes

- [release] restore the npm publish lane (`NPM_PUBLISH_ENABLED` flipped back to `'true'`); the CI validation DSH pin moves to 0.1.2-alpha.2
- [notes] record the drop of the superseded relative-cohort tarball store decision
- [notes] record the alpha.2 exclusion of external plugins decision
- [sdk] regenerate the lockfile at the main checkout depth
- [community-plugins] register dsh-agent-plugins-market in the community plugin index
- [notes] record the four-registration verdicts maintenance run
- [notes] record the two-tier acceptance policy and four-registration landing
- [notes] satisfy the no-emoji gate in the turnstile pairing note
- [readme] sync the contributor list from the GitHub API
- [notes] record the owner verdict on the dropped dsh-memory registration

---

Generated automatically by the release pipeline (scripts/release-notes.mjs).

</details>

---

由发布管线自动生成（scripts/release-notes.mjs）。
