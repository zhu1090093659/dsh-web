//#region src/invariant.ts
/** Invariant companion plugin (no assertions — nothing to check at runtime). */
/** Provides no assertions: the branch view owns no cross-package runtime invariants. */
function apply() {}
//#endregion
export { apply };
