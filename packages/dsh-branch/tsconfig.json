{
  // Solution file: one program per side (host half vs browser half). The
  // host `sessions` merge (dsh-workspace root) and the browser `sessions`
  // merge (client runtime) cannot share a ts.Program (TS2717).
  "files": [],
  "references": [
    { "path": "./tsconfig.host.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
