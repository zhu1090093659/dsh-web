interface:
  display_name: "Use DSH Store"
  short_description: "Search and manage DSH Store projects"
  default_prompt: "Use $search-dsh-store to search, inspect, or handle a DSH Store lifecycle request within its API and safety boundaries."
