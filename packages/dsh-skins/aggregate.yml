# 聚合包清单：patchFrom 的包其 cordis.patch.yml insert 行会被汇总进本包 patch
# deps 的包进 package.json dependencies（workspace:*）
# 皮肤包不进 patchFrom：皮肤启用互斥由 dsh-skin use 管理（~/.dsh/cordis.patch.yml managed 区段）
patchFrom:
  - ../skins/skin-center
deps:
  - ../skins/skin-center
  - ../skins/qq98
  - ../skins/ths
  - ../skins/xp
  - ../skins/blue-fantasy
  - ../skins/dragon-heir
  - ../skins/minecraft
  - ../skins/miku
  - ../skins/trading
