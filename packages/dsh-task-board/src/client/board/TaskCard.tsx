/** * Task card: the board's column item. Clicking opens the task detail — it * never executes anything directly (detail holds the Run button). */import type { TaskRecord, TaskStatus } from '../../core/tasks.ts'import { executionLabel } from '../../core/tasks.ts'import { t } from '../locales.ts'import css from '../board.module.css'/** Compact relative/absolute time label. */export function formatTime(ms: number): string {  const date = new Date(ms)  const now = Date.now()  const minutes = Math.floor((now - ms) / 60000)  if (minutes < 1) return t('time.justNow')  if (minutes < 60) return `${minutes}m`  if (minutes < 60 * 24) return `${Math.floor(minutes / 60)}h`  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`}/** MIME type + payload shape for card drags. */export const CARD_DRAG_TYPE = 'application/x-dsh-task-board-card'/** Card drag payload (set on dragstart, read on column drop). */export interface CardDragPayload {  taskId: string  fromStatus: TaskStatus}/** One card in a column. */export function TaskCard({  task, onClick, onDragStart, dragging = false,}: {  task: TaskRecord  onClick: () => void  onDragStart: (payload: CardDragPayload) => void  /** While the card is being dragged (visual feedback). */  dragging?: boolean}) {  const latest = task.executions[task.executions.length - 1]  const runs = task.executions.length  return (    <button      type="button"      className={css.card}      data-status={task.status}      data-task-id={task.id}      data-dragging={dragging || undefined}      draggable      onClick={onClick}      onDragStart={(event) => {        const payload: CardDragPayload = { taskId: task.id, fromStatus: task.status }        event.dataTransfer.setData(CARD_DRAG_TYPE, JSON.stringify(payload))        event.dataTransfer.effectAllowed = 'move'        // Keep the browser from using the button text as the drag image.        onDragStart(payload)      }}      title={task.description !== '' ? task.description : task.title}    >      <span className={css.cardTitle}>{task.title}</span>      {task.description !== '' && <span className={css.cardExcerpt}>{task.description}</span>}      <span className={css.cardMeta}>        <span className={css.cardTime}>{t('board.updated')} {formatTime(task.updatedAt)}</span>        {task.schedule?.enabled === true && (          <span            className={css.cardSchedule}            title={task.schedule.nextRunAt !== undefined              ? `${t('card.scheduled')} · ${new Date(task.schedule.nextRunAt).toLocaleString()}`              : t('card.scheduled')}          >            {t('card.scheduled')}          </span>        )}        {latest !== undefined && (          <span className={css.cardRun} data-result={latest.result}>            {runs} {t('board.runs')}          </span>        )}        {latest?.sessionId !== undefined && (          <span className={css.cardSession} title={latest.sessionId}>⌁</span>        )}        {task.status === 'running' && <span className={css.cardSpinner} aria-hidden="true" />}      </span>      {latest !== undefined && executionLabel(latest) === 'running' && (        <span className={css.cardRunningLabel}>{t('detail.result.running')}…</span>      )}    </button>  )}